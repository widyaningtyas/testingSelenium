pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {
                sh 'clean install'
                withMaven {
                    sh 'mvn clean verify'
                }
            }
        }
    }
}
