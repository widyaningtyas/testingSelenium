pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {
                echo 'mvn -v'
                echo 'which mvn'
                withMaven {

                    sh './mvnw clean verify'
                }
            }
        }
    }
}
