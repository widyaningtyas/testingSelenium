pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {
                sh 'mvn -v'
                sh 'which mvn'
                withMaven {

                    sh './mvnw clean verify'
                }
            }
        }
    }
}
