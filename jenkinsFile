pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {

                withMaven(maven: '3.8.2') {
                    sh 'mvn -v'
                    sh 'which mvn'
                    sh './mvnw clean verify'
                }
            }
        }
    }
}
