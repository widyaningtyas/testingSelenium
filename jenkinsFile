pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {
                withMaven {
                    sh 'mvn clean verify'
                }
            }
        }
    }
}
