pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {

                withMaven(maven: '3.9.0') {
                    sh 'mvn -v'
                    sh 'which mvn'
                    sh './mvnw clean verify'
                }
            }
        }
    }
}
