pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Run test') {
            steps {
                 sh 'mvn clean verify'
            }
        }
    }
}
